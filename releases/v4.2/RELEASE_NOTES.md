# JellyUpload v4.2 — Kurzfassung

Veröffentlicht: (siehe Blog)

Kurz: UI-Fixes — schnelle, gezielte UI-Updates nach Edits ohne kompletten Re-Render; neue Funktion `updateFileUIAfterEdit()`. Siehe `Blog/v4.2` für Details.

Enthalten: `app.js`, `app.json`, `index.html`, `style.css`
